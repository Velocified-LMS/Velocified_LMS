import Navbar from "@/app/components/navbar";
import { Padding } from "@mui/icons-material";
const Learner = () => {
  return (
    <div>
        <Navbar />
        <h1 style={{ position: 'absolute', top: '50%', left:"50%", color: 'red', fontSize: '24px' }}>About US Template</h1>
    </div>
  );
};

export default Learner;